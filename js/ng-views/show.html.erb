<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <p>{{vm.entry.author}}</p>
    <div><img src "{{vm.entry.photo_url}}" alt="{{vm.entry.author}}"= </div>
    <p>{{vm.entry.body}}</p>

  </body>
</html>
